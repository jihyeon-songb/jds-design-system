{
  "name": "@jds/storybook",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "storybook build",
    "storybook": "storybook dev -p 6006",
    "test": "test-storybook",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "lint": "tsc --noEmit -p tsconfig.json"
  },
  "dependencies": {
    "@jds/components": "workspace:*",
    "@jds/tokens": "workspace:*"
  },
  "devDependencies": {
    "@storybook/test-runner": "^0.24.4",
    "playwright": "^1.62.0"
  }
}
