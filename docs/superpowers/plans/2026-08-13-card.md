# Card Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** `@jdsb/components`에 native HTML 의미와 ref를 보존하는 token 기반의 비상호작용 Card 조합 프리미티브를 추가한다.

**Architecture:** `Card` 계열은 각각 하나의 native element를 forwardRef로 렌더링하는 작은 컴포넌트다. `CardTitle`만 generic `as` prop으로 기본 `h3`를 바꾸며, 상태·상호작용·자동 ARIA 관계는 도입하지 않는다. Card에 필요한 surface, content, spacing token을 먼저 만들고 CSS와 package entry로 공개 표면을 연결한다.

**Tech Stack:** React 19, TypeScript, CSS custom properties, Vitest, Testing Library, Storybook 10.

## Global Constraints

- 외부 UI 라이브러리와 새 의존성을 추가하지 않는다.
- Card는 비상호작용 `div` 프리미티브다. keyboard handler, tabindex, role, focus management, variant, size API를 추가하지 않는다.
- `CardTitle`의 기본 요소는 `h3`이고, `as`로 소비자의 문서 outline에 맞는 native 요소를 지정할 수 있다.
- 시각 값은 `@jdsb/tokens` CSS 변수만 사용한다. CSS에 색상·간격·반경·크기의 리터럴을 쓰지 않는다.
- 모든 root는 consumer `className`, native 속성, 이벤트, ref를 보존한다.
- Card와 CardTitle은 자동 landmark나 `aria-labelledby` 관계를 만들지 않는다.

---

## File Structure

- Create: `packages/components/src/layout/Card.tsx` — 공개 Card 조합 API와 native ref 전달
- Create: `packages/components/src/layout/Card.css` — Card 계열의 token-only 스타일
- Create: `packages/components/src/layout/Card.test.tsx` — native markup, ref, `as`, package export 회귀 검사
- Create: `apps/storybook/src/layout/Card.stories.tsx` — 기본 조합, heading level, footer action 문서 및 axe 대상
- Modify: `packages/tokens/src/jdsb.tokens.json` — Card surface/content/spacing token 원본
- Modify: `packages/components/src/index.ts` — Card public JavaScript/type export
- Modify: `packages/components/src/index.css` — Card stylesheet import
- Modify: `packages/components/package.json` — Card stylesheet 배포 allowlist

### Task 1: Card token과 native 조합 API

**Files:**

- Modify: `packages/tokens/src/jdsb.tokens.json`
- Create: `packages/components/src/layout/Card.tsx`
- Create: `packages/components/src/layout/Card.test.tsx`

**Interfaces:**

- Produces: `CardProps`, `CardHeaderProps`, `CardDescriptionProps`, `CardContentProps`, `CardFooterProps`, each `ComponentPropsWithoutRef<"div">`.
- Produces: `CardTitleProps<T extends ElementType = "h3"> = { as?: T } & Omit<ComponentPropsWithoutRef<T>, "as">`.
- Produces: `Card`, `CardHeader`, `CardDescription`, `CardContent`, `CardFooter` with `HTMLDivElement` refs; `CardTitle` with the ref type of its selected `as` element.
- Produces: `--jdsb-color-card-background`, `--jdsb-color-card-foreground`, `--jdsb-color-card-border`, `--jdsb-color-card-description`, `--jdsb-space-card-inline`, `--jdsb-space-card-block`, and `--jdsb-space-card-gap` through the existing token build.

- [ ] **Step 1: Write the failing API tests**

Create `packages/components/src/layout/Card.test.tsx`:

```tsx
import { createRef } from "react"
import { cleanup, render, screen } from "@testing-library/react"
import { afterEach, describe, expect, it } from "vitest"
import {
  Card,
  CardContent,
  CardDescription,
  CardFooter,
  CardHeader,
  CardTitle,
} from "./Card.js"

afterEach(cleanup)

describe("Card", () => {
  it("forwards Card div props and its ref", () => {
    const ref = createRef<HTMLDivElement>()
    render(<Card aria-label="월간 사용량" className="consumer-card" id="usage" ref={ref} />)

    expect(ref.current).toBe(screen.getByLabelText("월간 사용량"))
    expect(ref.current).toHaveAttribute("id", "usage")
    expect(ref.current).toHaveClass("jdsb-card", "consumer-card")
  })

  it("renders the structural primitives as div elements", () => {
    render(
      <Card>
        <CardHeader data-testid="header"><CardDescription>설명</CardDescription></CardHeader>
        <CardContent data-testid="content">본문</CardContent>
        <CardFooter data-testid="footer">동작</CardFooter>
      </Card>
    )

    expect(screen.getByTestId("header").tagName).toBe("DIV")
    expect(screen.getByText("설명").tagName).toBe("DIV")
    expect(screen.getByTestId("content").tagName).toBe("DIV")
    expect(screen.getByTestId("footer").tagName).toBe("DIV")
  })

  it("uses h3 by default and supports a native heading override", () => {
    const { rerender } = render(<CardTitle>기본 제목</CardTitle>)
    expect(screen.getByRole("heading", { level: 3, name: "기본 제목" })).toHaveClass("jdsb-card-title")

    rerender(<CardTitle as="h2">섹션 제목</CardTitle>)
    expect(screen.getByRole("heading", { level: 2, name: "섹션 제목" })).toHaveClass("jdsb-card-title")
  })
})
```

- [ ] **Step 2: Run the test to verify it fails**

Run: `pnpm test packages/components/src/layout/Card.test.tsx`

Expected: FAIL — `./Card.js` cannot be resolved.

- [ ] **Step 3: Add only Card semantic tokens**

In `packages/tokens/src/jdsb.tokens.json`, add `color.card` and `space.card` beside the existing semantic groups:

```json
"card": {
  "background": { "$value": "{color.action.outline.background}", "$type": "color" },
  "foreground": { "$value": "{color.action.outline.foreground}", "$type": "color" },
  "border": { "$value": "{color.action.outline.border}", "$type": "color" },
  "description": { "$value": "{color.neutral.700}", "$type": "color" }
}
```

```json
"card": {
  "inline": { "$value": "{space.button.inline}", "$type": "dimension" },
  "block": { "$value": "{space.field.item}", "$type": "dimension" },
  "gap": { "$value": "{space.field.content}", "$type": "dimension" }
}
```

Run: `pnpm --filter @jdsb/tokens build`

Expected: PASS and the generated `packages/tokens/dist/tokens.css` contains the seven Card variables. Do not hand-edit `dist` files.

- [ ] **Step 4: Implement the smallest native components**

Create `packages/components/src/layout/Card.tsx`:

```tsx
import {
  forwardRef,
  type ComponentPropsWithoutRef,
  type ComponentPropsWithRef,
  type ElementType,
  type ReactElement,
} from "react"

const classes = (base: string, className?: string) => [base, className].filter(Boolean).join(" ")

export type CardProps = ComponentPropsWithoutRef<"div">
export type CardHeaderProps = ComponentPropsWithoutRef<"div">
export type CardDescriptionProps = ComponentPropsWithoutRef<"div">
export type CardContentProps = ComponentPropsWithoutRef<"div">
export type CardFooterProps = ComponentPropsWithoutRef<"div">
export type CardTitleProps<T extends ElementType = "h3"> = { as?: T } & Omit<ComponentPropsWithoutRef<T>, "as">

export const Card = forwardRef<HTMLDivElement, CardProps>(function Card({ className, ...props }, ref) {
  return <div {...props} ref={ref} className={classes("jdsb-card", className)} />
})

export const CardHeader = forwardRef<HTMLDivElement, CardHeaderProps>(function CardHeader({ className, ...props }, ref) {
  return <div {...props} ref={ref} className={classes("jdsb-card-header", className)} />
})

type CardTitleComponent = <T extends ElementType = "h3">(
  props: CardTitleProps<T> & { ref?: ComponentPropsWithRef<T>["ref"] },
) => ReactElement | null

export const CardTitle = forwardRef(function CardTitle<T extends ElementType = "h3">(
  { as, className, ...props }: CardTitleProps<T>,
  ref: ComponentPropsWithRef<T>["ref"],
) {
  const Component = as ?? "h3"
  return <Component {...props} ref={ref} className={classes("jdsb-card-title", className)} />
}) as CardTitleComponent

export const CardDescription = forwardRef<HTMLDivElement, CardDescriptionProps>(function CardDescription({ className, ...props }, ref) {
  return <div {...props} ref={ref} className={classes("jdsb-card-description", className)} />
})

export const CardContent = forwardRef<HTMLDivElement, CardContentProps>(function CardContent({ className, ...props }, ref) {
  return <div {...props} ref={ref} className={classes("jdsb-card-content", className)} />
})

export const CardFooter = forwardRef<HTMLDivElement, CardFooterProps>(function CardFooter({ className, ...props }, ref) {
  return <div {...props} ref={ref} className={classes("jdsb-card-footer", className)} />
})
```

Preserve `className` exactly once and let consumer event handlers run through native props.

- [ ] **Step 5: Verify the component test and typecheck pass**

Run: `pnpm test packages/components/src/layout/Card.test.tsx && pnpm --filter @jdsb/components typecheck`

Expected: PASS.

- [ ] **Step 6: Commit the API and tokens**

```bash
git add packages/tokens/src/jdsb.tokens.json packages/tokens/dist packages/components/src/layout/Card.tsx packages/components/src/layout/Card.test.tsx
git commit -m "feat: Card 조합 API 추가"
```

### Task 2: Card stylesheet, public surface, and Storybook

**Files:**

- Create: `packages/components/src/layout/Card.css`
- Create: `apps/storybook/src/layout/Card.stories.tsx`
- Modify: `packages/components/src/index.ts`
- Modify: `packages/components/src/index.css`
- Modify: `packages/components/package.json`
- Modify: `packages/components/src/layout/Card.test.tsx`

**Interfaces:**

- Consumes: Card symbols and the seven generated Card CSS variables from Task 1.
- Produces: public imports of the six Card symbols and their prop types from `@jdsb/components` and their styles from `@jdsb/components/css`.

- [ ] **Step 1: Extend the failing test for the public export**

Add to `packages/components/src/layout/Card.test.tsx`:

```tsx
import { Card as PublicCard, type CardProps as PublicCardProps } from "../index.js"

it("exports Card and its props from the package entry", () => {
  const props: PublicCardProps = { children: "사용량" }
  render(<PublicCard {...props} />)

  expect(PublicCard).toBe(Card)
  expect(screen.getByText("사용량")).toHaveClass("jdsb-card")
})
```

- [ ] **Step 2: Run the test to verify it fails**

Run: `pnpm test packages/components/src/layout/Card.test.tsx`

Expected: FAIL — `Card` is not exported by `../index.js`.

- [ ] **Step 3: Add token-only CSS and package wiring**

Create `packages/components/src/layout/Card.css`:

```css
.jdsb-card {
  background: var(--jdsb-color-card-background);
  border: var(--jdsb-size-border) solid var(--jdsb-color-card-border);
  border-radius: var(--jdsb-radius-control);
  color: var(--jdsb-color-card-foreground);
}

.jdsb-card-header,
.jdsb-card-content,
.jdsb-card-footer {
  padding: var(--jdsb-space-card-block) var(--jdsb-space-card-inline);
}

.jdsb-card-header {
  display: grid;
  gap: var(--jdsb-space-card-gap);
}

.jdsb-card-title,
.jdsb-card-description {
  margin: 0;
}

.jdsb-card-title {
  font: inherit;
}

.jdsb-card-description {
  color: var(--jdsb-color-card-description);
}

.jdsb-card-footer {
  display: flex;
  gap: var(--jdsb-space-card-gap);
}

@media (forced-colors: active) {
  .jdsb-card {
    forced-color-adjust: auto;
  }
}
```

Append this exact export group to `packages/components/src/index.ts`:

```ts
export {
  Card,
  CardContent,
  CardDescription,
  CardFooter,
  CardHeader,
  CardTitle,
  type CardContentProps,
  type CardDescriptionProps,
  type CardFooterProps,
  type CardHeaderProps,
  type CardProps,
  type CardTitleProps,
} from "./layout/Card.js"
```

Add `@import "./layout/Card.css";` to `packages/components/src/index.css` and add
`"src/layout/Card.css"` to the `files` array in `packages/components/package.json`.

Do not add a CSS reset. `margin: 0` applies only to Card title/description so the primitive is
self-contained; all visual values remain token-backed or the inherited font.

- [ ] **Step 4: Create Storybook coverage**

Create `apps/storybook/src/layout/Card.stories.tsx`:

```tsx
import type { Meta, StoryObj } from "@storybook/react-vite"
import { Button, Card, CardContent, CardDescription, CardFooter, CardHeader, CardTitle } from "@jdsb/components"

const meta = { title: "Layout/Card", component: Card } satisfies Meta<typeof Card>

export default meta
type Story = StoryObj<typeof meta>

export const Default: Story = {
  render: () => (
    <Card>
      <CardHeader>
        <CardTitle>월간 사용량</CardTitle>
        <CardDescription>이번 결제 주기의 누적 사용량입니다.</CardDescription>
      </CardHeader>
      <CardContent>8.4 GB / 10 GB</CardContent>
    </Card>
  ),
}

export const SectionHeading: Story = {
  render: () => <Card><CardHeader><CardTitle as="h2">프로젝트</CardTitle></CardHeader></Card>,
}

export const WithFooterAction: Story = {
  render: () => <Card><CardContent>변경 사항을 검토하세요.</CardContent><CardFooter><Button>검토하기</Button></CardFooter></Card>,
}
```

- [ ] **Step 5: Verify package, repository, and Storybook checks**

Run:

```bash
pnpm typecheck
pnpm test
pnpm build
pnpm lint
pnpm --filter @jdsb/storybook build
```

Expected: every command exits 0. If a local Storybook server is available, also run
`pnpm --filter @jdsb/storybook test`; axe reports no violations for the Card stories.

- [ ] **Step 6: Commit the public surface and documentation**

```bash
git add packages/components/src/layout/Card.css packages/components/src/layout/Card.test.tsx packages/components/src/index.ts packages/components/src/index.css packages/components/package.json apps/storybook/src/layout/Card.stories.tsx
git commit -m "feat: Card 스타일과 문서 추가"
```

## Self-Review

- Spec coverage: Task 1 delivers all six native components, ref forwarding, title level choice, and Card-only semantic tokens. Task 2 delivers token-only styling, public CSS/JS surface, Storybook, axe coverage, and repository verification.
- Scope: no interaction state, click behavior, variants, media slots, automatic ARIA relationship, or dependency is introduced.
- Type consistency: Task 1 defines every prop and component exported in Task 2. Task 2 imports exactly those six symbols in the story.
- Placeholder scan: no unresolved choices or deferred implementation steps remain.
