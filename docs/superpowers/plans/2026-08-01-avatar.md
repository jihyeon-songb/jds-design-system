# Avatar 구현 계획

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**목표:** 이미지가 없거나 실패해도 이름의 첫 Unicode 문자로 식별할 수 있는 token 기반의 비상호작용 `Avatar`를 `@jds/components`에 추가한다.

**아키텍처:** `Avatar`는 native span root 안에 이미지를 표시하고, `src`별 실패 상태만 보관한다. 실패한 src와 다른 src는 곧바로 새 이미지를 시도하며, fallback은 `Array.from(name)[0]`로 계산한다. 색상·크기·원형 반경은 Avatar token만 사용하고 package entry와 CSS entry에서 public surface를 연결한다.

**기술 스택:** React 19, TypeScript, CSS custom properties, Vitest, Testing Library, Storybook 10.

## Global Constraints

- 외부 UI 라이브러리와 새 의존성을 추가하지 않는다.
- Avatar는 native `<span>` 기반의 비상호작용 표현 요소다. keyboard handler, focus management, loading spinner, retry, status badge, group API를 추가하지 않는다.
- `size`는 `"sm" | "md" | "lg" | "xl"`이고 기본값은 `"md"`다.
- 표시 순서는 성공한 `src` 이미지, `name`의 첫 Unicode 문자, 빈 원형이다.
- `src` 변경은 새 이미지를 다시 시도해야 하며, `useEffect`로 실패 상태를 초기화하지 않는다.
- 시각 값은 `@jds/tokens` CSS 변수만 사용한다.

---

## 파일 구조

- Create: `packages/components/src/feedback/Avatar.tsx` — Avatar 공개 API, 이미지 실패 fallback, native span root
- Create: `packages/components/src/feedback/Avatar.css` — token-only Avatar 표현
- Create: `packages/components/src/feedback/Avatar.test.tsx` — fallback·접근성·public export 회귀 검사
- Create: `apps/storybook/src/feedback/Avatar.stories.tsx` — 이미지·fallback·크기 문서와 axe 대상
- Modify: `packages/tokens/src/jds.tokens.json` — Avatar 색상·크기·반경 token 원본
- Modify: `packages/tokens/dist/tokens.css`, `packages/tokens/dist/index.js`, `packages/tokens/dist/index.d.ts` — token build 생성물
- Modify: `packages/components/src/index.ts`, `packages/components/src/index.css`, `packages/components/package.json` — public JS/CSS export

### Task 1: Avatar native API, fallback, 접근성 테스트

**Files:**

- Create: `packages/components/src/feedback/Avatar.tsx`
- Create: `packages/components/src/feedback/Avatar.test.tsx`

**Interfaces:**

- Produces: `AvatarSize = "sm" | "md" | "lg" | "xl"`.
- Produces: `AvatarProps = Omit<ComponentPropsWithoutRef<"span">, "children"> & { alt?: string; name?: string; size?: AvatarSize; src?: string }`.
- Produces: `Avatar`, a forward-ref component whose root has `class="jds-avatar"` and `data-size`.

- [ ] **Step 1: Write the failing API and fallback tests**

Create `packages/components/src/feedback/Avatar.test.tsx`:

```tsx
import { createRef } from "react"
import { cleanup, fireEvent, render, screen } from "@testing-library/react"
import { afterEach, describe, expect, it } from "vitest"
import { Avatar } from "./Avatar.js"

afterEach(cleanup)

describe("Avatar", () => {
  it("uses md by default and forwards span props and its ref", () => {
    const ref = createRef<HTMLSpanElement>()
    render(<Avatar aria-label="작성자" className="consumer-avatar" id="author" ref={ref} />)

    expect(ref.current).toBe(screen.getByLabelText("작성자"))
    expect(ref.current).toHaveAttribute("id", "author")
    expect(ref.current).toHaveClass("jds-avatar", "consumer-avatar")
    expect(ref.current).toHaveAttribute("data-size", "md")
  })

  it("renders an image with explicit alt text", () => {
    render(<Avatar alt="프로필 사진" name="김지현" src="/jihyeon.png" />)

    expect(screen.getByRole("img", { name: "프로필 사진" })).toHaveAttribute("src", "/jihyeon.png")
  })

  it("uses the name as image alt text when alt is absent", () => {
    render(<Avatar name="김지현" src="/jihyeon.png" />)

    expect(screen.getByRole("img", { name: "김지현" })).toHaveAttribute("src", "/jihyeon.png")
  })

  it("renders the first Unicode name character as an accessible fallback", () => {
    render(<Avatar name="😀지현" />)

    expect(screen.getByRole("img", { name: "😀지현" })).toHaveTextContent("😀")
    expect(screen.getByText("😀")).toHaveAttribute("aria-hidden", "true")
  })

  it("falls back after an image error and retries a different src", () => {
    const { rerender } = render(<Avatar name="김지현" src="/broken.png" />)
    fireEvent.error(screen.getByRole("img", { name: "김지현" }))

    expect(screen.getByRole("img", { name: "김지현" })).toHaveTextContent("김")

    rerender(<Avatar name="김지현" src="/replaced.png" />)
    expect(screen.getByRole("img", { name: "김지현" })).toHaveAttribute("src", "/replaced.png")
  })

  it.each(["sm", "md", "lg", "xl"] as const)("renders the %s size", (size) => {
    render(<Avatar aria-label={`${size} avatar`} size={size} />)

    expect(screen.getByLabelText(`${size} avatar`)).toHaveAttribute("data-size", size)
  })

  it("keeps consumer-provided ARIA semantics for an empty avatar", () => {
    render(<Avatar aria-label="알 수 없는 사용자" role="status" />)

    expect(screen.getByRole("status")).toHaveAttribute("aria-label", "알 수 없는 사용자")
  })
})
```

- [ ] **Step 2: Run the test to verify it fails**

Run: `pnpm test packages/components/src/feedback/Avatar.test.tsx`

Expected: FAIL — `./Avatar.js` cannot be resolved.

- [ ] **Step 3: Implement the minimum Avatar component**

Create `packages/components/src/feedback/Avatar.tsx`:

```tsx
import { forwardRef, useState, type ComponentPropsWithoutRef } from "react"

export type AvatarSize = "sm" | "md" | "lg" | "xl"

export type AvatarProps = Omit<ComponentPropsWithoutRef<"span">, "children"> & {
  alt?: string
  name?: string
  size?: AvatarSize
  src?: string
}

export const Avatar = forwardRef<HTMLSpanElement, AvatarProps>(function Avatar(
  { alt, className, name, size = "md", src, ...props },
  ref
) {
  const [failedSrc, setFailedSrc] = useState<string>()
  const showImage = Boolean(src) && src !== failedSrc
  const fallback = name ? Array.from(name)[0] : undefined

  return (
    <span
      {...props}
      ref={ref}
      className={["jds-avatar", className].filter(Boolean).join(" ")}
      data-size={size}
      {...(!showImage && fallback && props.role === undefined && props["aria-labelledby"] === undefined && props["aria-label"] === undefined
        ? { role: "img", "aria-label": name }
        : {})}
    >
      {showImage ? <img alt={alt ?? name ?? ""} onError={() => setFailedSrc(src)} src={src} /> : null}
      {!showImage && fallback ? <span aria-hidden="true">{fallback}</span> : null}
    </span>
  )
})
```

Keep `failedSrc` as the only state: equality with the current `src` makes a new source render without a reset effect. Do not create a fallback component or image-prop API. The automatic `role="img"` and name only apply to a name fallback when the consumer did not provide a conflicting root semantic.

- [ ] **Step 4: Verify the component test and typecheck pass**

Run: `pnpm test packages/components/src/feedback/Avatar.test.tsx && pnpm --filter @jds/components typecheck`

Expected: PASS.

- [ ] **Step 5: Commit the native API**

Run: `git add packages/components/src/feedback/Avatar.tsx packages/components/src/feedback/Avatar.test.tsx && git commit -m "feat: Avatar API 추가"`

### Task 2: Avatar token, CSS, package surface

**Files:**

- Modify: `packages/tokens/src/jds.tokens.json`
- Modify: `packages/tokens/dist/tokens.css`
- Modify: `packages/tokens/dist/index.js`
- Modify: `packages/tokens/dist/index.d.ts`
- Create: `packages/components/src/feedback/Avatar.css`
- Modify: `packages/components/src/feedback/Avatar.test.tsx`
- Modify: `packages/components/src/index.ts`
- Modify: `packages/components/src/index.css`
- Modify: `packages/components/package.json`

**Interfaces:**

- Consumes: `Avatar`, `AvatarProps`, and `AvatarSize` from Task 1.
- Produces: `color.avatar.{background,foreground}`, `size.avatar.{sm,md,lg,xl}`, and `radius.avatar` CSS variables.
- Produces: public exports `Avatar`, `AvatarProps`, and `AvatarSize` from `@jds/components`.

- [ ] **Step 1: Add the failing package-entry test**

Add this import and test to `packages/components/src/feedback/Avatar.test.tsx`:

```tsx
import { Avatar as PublicAvatar, type AvatarProps as PublicAvatarProps } from "../index.js"

it("exports Avatar from the package entry", () => {
  const props: PublicAvatarProps = { name: "김지현" }
  render(<PublicAvatar {...props} />)

  expect(screen.getByRole("img", { name: "김지현" })).toHaveClass("jds-avatar")
})
```

- [ ] **Step 2: Run the test to verify the export is absent**

Run: `pnpm test packages/components/src/feedback/Avatar.test.tsx`

Expected: FAIL — `../index.js` does not export `Avatar`.

- [ ] **Step 3: Add source tokens, CSS, and package exports**

Add this `avatar` group at the top level of `color` in `packages/tokens/src/jds.tokens.json`:

```json
"avatar": {
  "background": { "$value": "{color.action.secondary.background}", "$type": "color" },
  "foreground": { "$value": "{color.action.secondary.foreground}", "$type": "color" }
}
```

Add this group at the top level of `size`:

```json
"avatar": {
  "sm": { "$value": "{size.control.button.sm.height}", "$type": "dimension" },
  "md": { "$value": "{size.control.button.md.height}", "$type": "dimension" },
  "lg": { "$value": "{size.control.button.lg.height}", "$type": "dimension" },
  "xl": { "$value": "{size.control.button.xl.height}", "$type": "dimension" }
}
```

Add this token at the top level of `radius`:

```json
"avatar": { "$value": "9999px", "$type": "dimension" }
```

Create `packages/components/src/feedback/Avatar.css`:

```css
.jds-avatar {
  align-items: center;
  background: var(--jds-color-avatar-background);
  block-size: var(--jds-size-avatar-md);
  border-radius: var(--jds-radius-avatar);
  color: var(--jds-color-avatar-foreground);
  display: inline-flex;
  flex: none;
  inline-size: var(--jds-size-avatar-md);
  justify-content: center;
  overflow: hidden;
}

.jds-avatar[data-size="sm"] { block-size: var(--jds-size-avatar-sm); inline-size: var(--jds-size-avatar-sm); }
.jds-avatar[data-size="lg"] { block-size: var(--jds-size-avatar-lg); inline-size: var(--jds-size-avatar-lg); }
.jds-avatar[data-size="xl"] { block-size: var(--jds-size-avatar-xl); inline-size: var(--jds-size-avatar-xl); }

.jds-avatar > img {
  block-size: 100%;
  inline-size: 100%;
  object-fit: cover;
}

.jds-avatar > span { font: inherit; }

@media (forced-colors: active) {
  .jds-avatar { forced-color-adjust: auto; }
}
```

Append this export to `packages/components/src/index.ts`, import the CSS from `index.css`, and include the CSS file in the package `files` list:

```ts
export { Avatar, type AvatarProps, type AvatarSize } from "./feedback/Avatar.js"
```

```css
@import "./feedback/Avatar.css";
```

```json
"src/feedback/Avatar.css"
```

Regenerate, rather than manually editing, all three token dist files:

Run: `pnpm --filter @jds/tokens build`

- [ ] **Step 4: Verify tokens, public API, and build output**

Run: `pnpm test packages/components/src/feedback/Avatar.test.tsx && pnpm typecheck && pnpm build && pnpm lint`

Expected: PASS. `packages/tokens/dist/tokens.css` contains the Avatar variables, and the public-entry test renders `PublicAvatar`.

- [ ] **Step 5: Commit the visual and public surface**

Run: `git add packages/tokens/src/jds.tokens.json packages/tokens/dist packages/components/src/feedback/Avatar.css packages/components/src/feedback/Avatar.test.tsx packages/components/src/index.ts packages/components/src/index.css packages/components/package.json && git commit -m "feat: Avatar 토큰과 스타일 추가"`

### Task 3: Storybook 문서와 최종 검증

**Files:**

- Create: `apps/storybook/src/feedback/Avatar.stories.tsx`

**Interfaces:**

- Consumes: public `Avatar` and `AvatarSize` API from Task 2.
- Produces: `Feedback/Avatar` Stories that Storybook preview-level axe configuration evaluates.

- [ ] **Step 1: Add image, fallback, error, and size Stories**

Create `apps/storybook/src/feedback/Avatar.stories.tsx`:

```tsx
import type { Meta, StoryObj } from "@storybook/react-vite"
import { Avatar } from "@jds/components"

const meta = {
  title: "Feedback/Avatar",
  component: Avatar,
} satisfies Meta<typeof Avatar>

export default meta
type Story = StoryObj<typeof meta>

export const Image: Story = {
  args: { alt: "김지현", name: "김지현", src: "https://images.unsplash.com/photo-1494790108377-be9c29b29330?auto=format&fit=crop&w=128&h=128&q=80" },
}

export const NameFallback: Story = { args: { name: "김지현" } }
export const ImageErrorFallback: Story = { args: { name: "김지현", src: "/avatar-does-not-exist.png" } }
export const Empty: Story = { args: { "aria-label": "알 수 없는 사용자" } }

export const Sizes: Story = {
  render: () => (
    <div style={{ alignItems: "center", display: "flex", gap: "var(--jds-space-button-gap)" }}>
      <Avatar name="김지현" size="sm" />
      <Avatar name="김지현" size="md" />
      <Avatar name="김지현" size="lg" />
      <Avatar name="김지현" size="xl" />
    </div>
  ),
}
```

The remote image is documentation-only; it is not a test dependency. `ImageErrorFallback` deliberately uses a missing local path so a browser displays the same `onError` behavior covered by the component test. The Story's layout uses an existing CSS token and adds no component CSS value.

- [ ] **Step 2: Run automated verification**

Run: `pnpm typecheck && pnpm test && pnpm build && pnpm lint && pnpm --filter @jds/storybook build`

Expected: every command exits 0; Storybook builds every `Feedback/Avatar` Story with preview a11y mode set to `error`.

- [ ] **Step 3: Manually inspect visual and accessibility behavior**

Run: `pnpm --filter @jds/storybook storybook`

Expected: the Image, NameFallback, ImageErrorFallback, Empty, and Sizes Stories load. At 200% zoom and in forced-colors, Avatar remains circular, clipped, and readable. Verify no Avatar enters Tab order; image and name fallback expose their expected accessible names.

- [ ] **Step 4: Commit Storybook documentation**

Run: `git add apps/storybook/src/feedback/Avatar.stories.tsx && git commit -m "feat: Avatar Storybook 문서 추가"`

## Self-review

- Spec coverage: Task 1 implements the API, source-specific failure recovery, Unicode fallback, ref/native props, and fallback accessibility. Task 2 supplies token-only styling and package exports. Task 3 supplies Storybook/axe coverage and the required automated and manual checks.
- Placeholder scan: no TBD/TODO, unspecified test, or implicit implementation step remains.
- Type consistency: every later task uses the `Avatar`, `AvatarProps`, and `AvatarSize` produced by Task 1; the generated CSS variables exactly match Task 2 token names.
