# Input Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** `@jds/components`에 native 동작과 `sm`~`xl` 상태 스타일을 제공하는 접근 가능한 Input을 추가한다.

**Architecture:** Input은 native `<input>` 하나에 class와 stable data attribute만 추가한다. 기존 field 색상·focus·radius 토큰을 재사용하고 Input의 높이와 여백 토큰만 생성한다. Storybook은 기존 preview의 axe 구성으로 컴포넌트를 문서화한다.

**Tech Stack:** React 19, TypeScript 5, native CSS, Vitest, Testing Library, Storybook 10, pnpm workspace.

## Global Constraints

- native HTML과 표준 ARIA만 사용하고 외부 UI 라이브러리나 새 의존성을 추가하지 않는다.
- 공개 API에는 명시적 TypeScript 타입을 제공하고 ref 및 적용 가능한 native input 속성을 전달한다.
- `InputSize`는 `"sm" | "md" | "lg" | "xl"`이며 기본값은 `"md"`다.
- CSS에는 시각 값 리터럴을 쓰지 않고 `--jds-*` 토큰만 사용한다.
- `invalid`일 때만 `aria-invalid="true"`를 설정하고 `data-state` 우선순위는 disabled, readonly, invalid, idle이다.
- 값 변환·입력 제한·아이콘 슬롯·type별 동작·FormField API를 추가하지 않는다.
- WCAG 2.2 AA를 기준으로 focus-visible, keyboard, forced-colors, reduced-motion을 지원한다.
- 완료 전 `pnpm typecheck`, `pnpm test`, `pnpm build`, `pnpm lint`, Storybook build를 통과시킨다.

---

## File Structure

- Modify: `packages/tokens/src/jds.tokens.json` — Input 높이와 여백의 DTCG 토큰 원본.
- Create: `packages/components/src/Input.tsx` — Input 공개 API와 native input 렌더링.
- Create: `packages/components/src/Input.css` — Input 상태·크기 스타일.
- Create: `packages/components/src/Input.test.tsx` — native 속성, 값, 상태의 회귀 검사.
- Modify: `packages/components/src/index.ts` — Input 공개 export.
- Modify: `packages/components/src/index.css` — Input CSS를 공개 CSS entrypoint에 포함.
- Modify: `packages/components/tsconfig.json` — Input source를 typecheck 대상에 포함.
- Modify: `packages/components/package.json` — Input CSS를 npm publish files에 포함.
- Create: `apps/storybook/src/Input.stories.tsx` — Input Storybook과 axe 대상 Story.

### Task 1: Input 토큰과 스타일

**Files:**
- Modify: `packages/tokens/src/jds.tokens.json`
- Create: `packages/components/src/Input.css`
- Modify: `packages/components/src/index.css`
- Modify: `packages/components/package.json`

**Interfaces:**
- Consumes: `--jds-color-field-*`, `--jds-size-border`, `--jds-size-focus`, `--jds-radius-control`, `--jds-opacity-disabled`.
- Produces: `--jds-size-control-input-*-height`, `--jds-space-input-inline`, `.jds-input` CSS selectors, and the `@jds/components/css` entrypoint.

- [ ] **Step 1: Input size 토큰을 추가한다**

`packages/tokens/src/jds.tokens.json`의 `size.control` 아래에 Input 높이를, `space` 아래에 Input 가로 여백을 추가한다.

```json
"input": {
  "sm": { "height": { "$value": "32px", "$type": "dimension" } },
  "md": { "height": { "$value": "36px", "$type": "dimension" } },
  "lg": { "height": { "$value": "40px", "$type": "dimension" } },
  "xl": { "height": { "$value": "44px", "$type": "dimension" } }
}
```

```json
"input": {
  "inline": { "$value": "12px", "$type": "dimension" }
}
```

- [ ] **Step 2: 토큰 출력을 확인한다**

Run: `pnpm --filter @jds/tokens build`

Expected: `packages/tokens/dist/tokens.css`에 `--jds-size-control-input-sm-height`부터 `xl`과 `--jds-space-input-inline`이 생성된다.

- [ ] **Step 3: Input CSS를 작성한다**

`packages/components/src/Input.css`를 만들고 다음 기본 selector를 사용한다.

```css
.jds-input {
  background: var(--jds-color-field-background);
  border: var(--jds-size-border) solid var(--jds-color-field-border);
  border-radius: var(--jds-radius-control);
  color: var(--jds-color-field-foreground);
  font: inherit;
  height: var(--jds-size-control-input-md-height);
  padding-inline: var(--jds-space-input-inline);
}

.jds-input[data-size="sm"] { height: var(--jds-size-control-input-sm-height); }
.jds-input[data-size="lg"] { height: var(--jds-size-control-input-lg-height); }
.jds-input[data-size="xl"] { height: var(--jds-size-control-input-xl-height); }
.jds-input[data-state="invalid"] { border-color: var(--jds-color-field-invalid-border); }
.jds-input:focus-visible { outline: var(--jds-size-focus) solid var(--jds-color-focus-ring); outline-offset: var(--jds-size-focus); }
```

같은 파일에 disabled·readOnly를 제외한 hover, disabled opacity, readOnly border, reduced-motion, forced-colors selector를 추가한다. Input type별 브라우저 UI를 바꾸는 selector는 넣지 않는다.

- [ ] **Step 4: CSS export와 publish files를 연결한다**

`packages/components/src/index.css`에 다음 import를 추가한다.

```css
@import "./Input.css";
```

`packages/components/package.json`의 `files` 배열에 `src/Input.css`를 추가한다. `exports["./css"]`는 기존 `./src/index.css`를 유지한다.

- [ ] **Step 5: 패키지 build를 확인한다**

Run: `pnpm --filter @jds/tokens build && pnpm --filter @jds/components build`

Expected: 두 build가 성공하고 새 CSS 변수와 Input CSS가 packages에 포함된다.

- [ ] **Step 6: 커밋한다**

```bash
git add packages/tokens/src/jds.tokens.json packages/components/src/Input.css packages/components/src/index.css packages/components/package.json
git commit -m "feat: Input 토큰과 스타일 추가"
```

### Task 2: Input 공개 API와 테스트

**Files:**
- Create: `packages/components/src/Input.tsx`
- Create: `packages/components/src/Input.test.tsx`
- Modify: `packages/components/src/index.ts`
- Modify: `packages/components/tsconfig.json`

**Interfaces:**
- Consumes: Task 1의 `.jds-input` selector, `data-size`, `data-state`와 Input 토큰 CSS 변수.
- Produces: `Input`, `InputProps`, `InputSize` from `@jds/components`.

- [ ] **Step 1: 실패하는 Input 테스트를 작성한다**

`packages/components/src/Input.test.tsx`에 다음 테스트를 작성한다.

```tsx
it("forwards the ref and native input props", () => {
  const ref = createRef<HTMLInputElement>()
  render(<Input ref={ref} aria-label="이메일" name="email" required type="email" />)

  expect(ref.current).toHaveAttribute("name", "email")
  expect(ref.current).toHaveAttribute("type", "email")
  expect(ref.current).toBeRequired()
})

it("preserves uncontrolled and controlled values", () => {
  const { rerender } = render(<Input aria-label="이름" defaultValue="하나" />)
  const control = screen.getByRole("textbox", { name: "이름" })
  fireEvent.change(control, { target: { value: "둘" } })
  expect(control).toHaveValue("둘")

  rerender(<Input aria-label="이름" value="셋" onChange={() => undefined} />)
  expect(control).toHaveValue("셋")
})
```

별도 테스트에서 `invalid`의 `aria-invalid`, disabled·readOnly·invalid의 우선순위별 `data-state`, 모든 size의 `data-size`를 검증한다.

- [ ] **Step 2: 테스트가 실패하는지 확인한다**

Run: `pnpm test -- packages/components/src/Input.test.tsx`

Expected: FAIL because `./Input.js`와 `Input` export가 아직 없다.

- [ ] **Step 3: 최소 Input 구현을 작성한다**

`packages/components/src/Input.tsx`에 다음 공개 타입을 작성한다.

```tsx
import { forwardRef, type ComponentPropsWithoutRef } from "react"

export type InputSize = "sm" | "md" | "lg" | "xl"

export type InputProps = ComponentPropsWithoutRef<"input"> & {
  size?: InputSize
  invalid?: boolean
}
```

`forwardRef<HTMLInputElement, InputProps>`로 `<input>` 하나를 렌더링한다. `className`은 `jds-input`과 공백으로 결합하고, `size="md"`, `invalid=false`를 기본값으로 둔다. `data-size`와 우선순위가 적용된 `data-state`를 설정하고 `invalid`일 때만 `aria-invalid`를 전달한다. 나머지 props, `value`, `defaultValue`, `onChange`, `aria-describedby`는 수정하지 않고 input에 전달한다.

`packages/components/src/index.ts`에 다음 export를 추가하고 `packages/components/tsconfig.json` include 배열에 `src/Input.tsx`를 추가한다.

```ts
export { Input, type InputProps, type InputSize } from "./Input.js"
```

- [ ] **Step 4: Input 테스트와 components typecheck를 통과시킨다**

Run: `pnpm test -- packages/components/src/Input.test.tsx && pnpm --filter @jds/components typecheck`

Expected: Input 테스트와 components typecheck가 PASS한다.

- [ ] **Step 5: 커밋한다**

```bash
git add packages/components/src/Input.tsx packages/components/src/Input.test.tsx packages/components/src/index.ts packages/components/tsconfig.json
git commit -m "feat: native Input 컴포넌트 추가"
```

### Task 3: Storybook 문서와 전체 검증

**Files:**
- Create: `apps/storybook/src/Input.stories.tsx`

**Interfaces:**
- Consumes: `Input`, `InputProps`, `@jds/components/css`, 기존 `preview.ts`의 axe error 설정.
- Produces: Input 기본·크기·상태·password 사용의 문서 Story.

- [ ] **Step 1: Input Story를 작성한다**

`apps/storybook/src/Input.stories.tsx`에 다음 메타데이터와 Story를 작성한다.

```tsx
const meta = {
  title: "Inputs/Input",
  component: Input,
  args: { "aria-label": "입력값", defaultValue: "" }
} satisfies Meta<typeof Input>

export const Default: Story = {}
export const Disabled: Story = { args: { disabled: true, defaultValue: "수정할 수 없습니다" } }
export const ReadOnly: Story = { args: { readOnly: true, defaultValue: "읽기 전용 값" } }
export const Invalid: Story = { args: { invalid: true } }
export const Password: Story = { args: { type: "password", defaultValue: "secret" } }
export const LongValue: Story = { args: { defaultValue: "긴 입력값도 native input에서 그대로 표시됩니다" } }
```

`Sizes` render Story에 `sm`, `md`, `lg`, `xl` Input을 각각 접근 가능한 이름과 함께 추가한다.

- [ ] **Step 2: Storybook typecheck와 build를 확인한다**

Run: `pnpm --filter @jds/storybook typecheck && pnpm --filter @jds/storybook build`

Expected: Input Story가 typecheck되고 Storybook static output이 axe 구성을 유지하며 생성된다.

- [ ] **Step 3: 전체 자동 검증을 실행한다**

Run: `pnpm typecheck && pnpm test && pnpm build && pnpm lint`

Expected: workspace typecheck, Vitest, packages build, lint가 모두 PASS한다.

- [ ] **Step 4: 수동 접근성 검사를 수행한다**

Storybook에서 다음을 확인한다.

1. Tab으로 각 Input에 포커스하고 focus ring이 보이는지 확인한다.
2. 키보드로 값 입력·선택·수정을 수행해 native 동작이 유지되는지 확인한다.
3. disabled와 readOnly의 편집 가능 여부가 native 동작과 일치하는지 확인한다.
4. 브라우저 확대, `prefers-reduced-motion`, forced-colors에서 내용과 focus 표시가 유지되는지 확인한다.

- [ ] **Step 5: 커밋한다**

```bash
git add apps/storybook/src/Input.stories.tsx
git commit -m "docs: Input Storybook 문서 추가"
```

## Self-Review

- Spec coverage: Task 1은 Input 토큰과 시각 상태, Task 2는 native API·값·ref·ARIA 상태, Task 3은 Storybook·axe·자동 및 수동 검증을 담당한다. 아이콘, type별 동작, FormField, 추가 의존성은 포함하지 않는다.
- Placeholder scan: 모든 파일, public type, token 이름, selector, 테스트 대상, 명령과 커밋 메시지를 명시했다.
- Type consistency: Task 2의 `Input`, `InputProps`, `InputSize`를 Task 3이 같은 이름으로 import한다. Task 1의 `.jds-input`, `data-size`, `data-state`를 Task 2 구현이 제공한다.
